node_modules/
dist/
.turbo/
*.tsbuildinfo

# Playwright artifacts (a stray run from the repo root drops test-results/ here)
test-results/
playwright-report/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.claude/

# Environment
.env
.env.*
!.env.example
!.env.template

# Observer state (local testing)
.observer/

# Per-operator deploy copies (live state, profile-specific tfvars).
# Only deploy/dev/ is canonical and tracked; sibling dirs are local.
/deploy/*/
!/deploy/dev/
