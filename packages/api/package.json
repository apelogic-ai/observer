{
  "name": "@observer/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "observer-api": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "bun src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo '(api: no dedicated lint config yet — typecheck is the gate)'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1039.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^22",
    "typescript": "^5.7",
    "vitest": "^3"
  }
}
