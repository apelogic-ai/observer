{
  "name": "@observer/agent",
  "version": "0.1.18",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "observer": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "compile": "bun build --compile src/cli.ts --outfile dist/observer",
    "dev": "bun src/cli.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo '(agent: no dedicated lint config yet — typecheck is the gate)'"
  },
  "dependencies": {
    "@observer/dashboard": "workspace:*",
    "commander": "^13.1.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^22",
    "typescript": "^5.7"
  }
}
